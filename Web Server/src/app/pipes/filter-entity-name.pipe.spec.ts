import { FilterEntityNamePipe } from './filter-entity-name.pipe';

describe('FilterEntityNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEntityNamePipe();
    expect(pipe).toBeTruthy();
  });
});
