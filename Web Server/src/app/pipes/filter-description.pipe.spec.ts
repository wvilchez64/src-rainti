import { FilterDescriptionPipe } from './filter-description.pipe';

describe('FilterDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
