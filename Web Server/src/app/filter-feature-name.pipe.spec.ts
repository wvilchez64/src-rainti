import { FilterFeatureNamePipe } from './filter-feature-name.pipe';

describe('FilterFeatureNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFeatureNamePipe();
    expect(pipe).toBeTruthy();
  });
});
